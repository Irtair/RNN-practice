[README.md](https://github.com/user-attachments/files/25368769/README.md)
# 🧠 Генерация текста твитов: LSTM vs Transformer

Проект по обучению пословной LSTM-модели для автодополнения текста на данных твитов, с последующим сравнением с предобученным трансформером `distilgpt2` через HuggingFace pipeline.

---

## 📁 Структура проекта

```
├── configs/
│   └── config.yaml             # Основной конфигурационный файл
├── data/                       # Сырые и обработанные данные
├── models/                     # Сохранённые чекпоинты модели
├── src/
│   ├── data_utils.py           # Загрузка, очистка, токенизация, датасет
│   ├── tokenizer.py            # Обучение WordLevel токенайзера
│   ├── lstm_model.py           # Архитектура LSTM модели
│   ├── lstm_train.py           # Обучение, валидация, оценка ROUGE
│   ├── eval_lstm.py            # Интерактивная генерация текста LSTM
│   └── eval_transformer_pipeline.py  # Генерация и ROUGE для трансформера
├── wordlevel.json              # Обученный токенайзер
├── solution.ipynb              # Основной ноутбук (весь пайплайн)
├── requirements.txt
└── .gitignore
```

---

## 🚀 Быстрый старт

### 1. Клонировать репозиторий

```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
python -m venv venv
source venv/bin/activate        # Linux/macOS
venv\Scripts\activate           # Windows

pip install -r requirements.txt
```

### 3. Настроить пути

Отредактируй `configs/config.yaml` — укажи пути к данным и задай гиперпараметры обучения.

---

## ⚙️ Пайплайн

Полный пайплайн запускается пошагово в `solution.ipynb`:

| Шаг | Описание |
|-----|----------|
| 1 | Загрузка и очистка сырых данных твитов |
| 2 | Обучение WordLevel токенайзера |
| 3 | Токенизация датасета |
| 4 | Разбивка на train / val / test |
| 5 | Обучение LSTM с ранней остановкой |
| 6 | Оценка по метрике ROUGE-1 |
| 7 | Сравнение с трансформером `distilgpt2` |

---

## 🏗️ Архитектура модели

Модель `AutoCompleteLSTM` включает:

- Слой эмбеддингов слов
- Многослойную LSTM с dropout
- LayerNorm + Dropout
- Линейный проекционный слой на словарь

Гиперпараметры задаются через `config.yaml` (`embedding_dim`, `hidden_dim`, `num_layers`, `dropout`).

---

## 📊 Оценка качества

Обе модели оцениваются по метрике **ROUGE-1** на тестовой выборке.

| Модель | ROUGE-1 |
|--------|---------|
| LSTM (обучена с нуля) | *твой результат* |
| distilgpt2 (предобученная, HuggingFace) | 0.0497 |

---

## 💬 Примеры генерации

**LSTM:**
```python
from src.eval_lstm import lsmt_generate
lsmt_generate("still dont know")
# → still dont know what to do
```

**Transformer:**
```python
from transformers import pipeline
from src.eval_transformer_pipeline import transformer_generate

generator = pipeline("text-generation", model="distilgpt2")
transformer_generate(generator, ["Deep Learning is", "What is going on with"])
```

---

## 📦 Зависимости

Основные библиотеки:
- `torch`
- `tokenizers`
- `transformers`
- `evaluate`
- `tqdm`
- `pyyaml`

Полный список — в `requirements.txt`.

---

## 📝 Примечания

- Токенайзер обучается на очищенном корпусе твитов по модели WordLevel с размером словаря 12 000 токенов.
- Перед обучением текст очищается: удаляются упоминания, ссылки и не-алфавитно-цифровые символы.
- Обучение LSTM использует оптимизатор `Adam`, функцию потерь `CrossEntropyLoss`, планировщик скорости обучения `ReduceLROnPlateau` и раннюю остановку.
